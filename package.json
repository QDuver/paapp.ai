{
  "name": "life-automation-codegen",
  "version": "1.0.0",
  "description": "Code generation for life automation project",
  "scripts": {
    "generate-models": "openapi-generator-cli generate -c openapitools.json --config-key models-only",
    "generate-models-only": "openapi-generator-cli generate -i models.yaml -g dart -o models --global-property models,supportingFiles=false --additional-properties=pubName=workout_models,pubDescription=\"Generated Dart models for Workout API\" --skip-validate-spec"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.21.4"
  }
}
